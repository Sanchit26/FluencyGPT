[project]
name = "fluencygpt"
version = "0.1.0"
description = "FluencyGPT: Automatic Stuttering Correction (offline demo-safe Flask backend)"
requires-python = ">=3.10"

[tool.pytest.ini_options]
testpaths = ["tests"]

[tool.ruff]
line-length = 100

[tool.black]
line-length = 100
